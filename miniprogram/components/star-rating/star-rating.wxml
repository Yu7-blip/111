<view class="star-wrap flex-row">
  <view class="star {{rating >= item ? 'active' : ''}}" 
        wx:for="{{[1, 2, 3, 4, 5]}}" 
        wx:key="*this" 
        bindtap="onTapStar" 
        data-val="{{item}}"
        style="font-size: {{size}}rpx;">
    ★
  </view>
</view>